<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <layer-list>
            <item> <!-- Shadow -->
                <shape>
                    <solid android:color="@color/venice_blue" />
                    <corners android:radius="@dimen/dp_2" />
                </shape>
            </item>
            <!-- BUTTON -->
            <item android:bottom="@dimen/dp_3">
                <shape>
                    <solid android:color="@color/venice_blue" />
                    <corners android:radius="@dimen/dp_2" />
                </shape>
            </item>
        </layer-list>
    </item>

    <!-- Disable state -->
    <item android:state_enabled="false">
        <layer-list>
            <item>  <!-- shadow -->
                <shape>
                    <solid android:color="#353535" />
                    <corners android:radius="@dimen/dp_2" />
                </shape>
            </item>
            <!-- BUTTON -->
            <item  android:bottom="@dimen/dp_3">
                <shape>
                    <solid android:color="#505050" />
                    <corners android:radius="@dimen/dp_2" />
                </shape>
            </item>
        </layer-list>
    </item>

    <!-- Default -->
    <item>
        <layer-list>
            <!-- SHADOW -->
            <item>
                <shape>
                    <solid android:color="#046ea3" />
                    <corners android:radius="@dimen/dp_2" />
                </shape>
            </item>
            <!-- BUTTON -->
            <item android:bottom="@dimen/dp_3">
                <shape android:shape="rectangle">
                    <solid android:color="#008ae1" />
                    <corners android:radius="@dimen/dp_2" />
                </shape>
            </item>
        </layer-list>
    </item>
</selector>